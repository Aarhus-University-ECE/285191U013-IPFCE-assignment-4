#include "get_min.h"

#include <assert.h>

/// Exercise 1
/// Returns the smallest of the first n values in the list
/// Pre: n > 0, list[0..n-1] is defined
int get_min(int list[], int n) {
  assert(n > 0);

  return 0;
}
