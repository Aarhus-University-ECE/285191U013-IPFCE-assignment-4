#include "average.h"

#include <assert.h>
/// Excercise 6
/// Returns the average of an array
/// Pre: n>0, list[0...n-1] is defined
double average(int list[], int n) {
  double avg = 0;
  assert(n > 0);
  //...
  return avg;
}
